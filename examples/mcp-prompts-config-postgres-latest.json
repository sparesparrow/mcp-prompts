{
  "mcpServers": {
    "mcp-prompts": {
      "command": "npx",
      "args": ["-y", "@sparesparrow/mcp-prompts"],
      "env": {
        "STORAGE_TYPE": "postgres",
        "POSTGRES_URL": "postgres://username:password@localhost:5432/mcp_prompts",
        "LOG_LEVEL": "info",
        "PORT": "3003",
        "HOST": "0.0.0.0"
      }
    }
  }
}
