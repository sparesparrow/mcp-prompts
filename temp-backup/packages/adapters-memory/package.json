{
  "name": "@sparesparrow/mcp-prompts-adapters-memory",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "swc src -d dist --copy-files",
    "build:watch": "swc src -d dist --watch --copy-files",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sparesparrow/mcp-prompts-core": "workspace:*"
  }
}
