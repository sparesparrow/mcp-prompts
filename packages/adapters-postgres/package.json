{
  "name": "@sparesparrow/mcp-prompts-adapters-postgres",
  "version": "0.1.0",
  "main": "dist/PostgresPromptRepository.js",
  "types": "dist/PostgresPromptRepository.d.ts",
  "type": "module",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "@sparesparrow/mcp-prompts-core": "workspace:*",
    "pg": "^8.11.3"
  },
  "scripts": {
    "build": "swc src -d dist --copy-files",
    "build:watch": "swc src -d dist --watch --copy-files",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/pg": "^8.15.4"
  }
}
