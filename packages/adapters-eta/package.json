{
  "name": "@mcp-prompts/adapters-eta",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "@mcp-prompts/core": "workspace:*"
  },
  "scripts": {
    "build": "swc src -d dist --copy-files",
    "build:watch": "swc src -d dist --watch --copy-files",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
